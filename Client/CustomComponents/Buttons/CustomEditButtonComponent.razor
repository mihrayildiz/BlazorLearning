@inherits BaseButtonComponent    


<ButtonComponent Caption="@(Caption ?? "Edit")"
                 Color="@(Color ?? "secondary")"
                 Icon="@(Icon ?? "pencil-square-o")"
                 OnClick="OnClick"
                 ToolTypeStr="@(ToolTypeStr ?? "Edit")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall"/>